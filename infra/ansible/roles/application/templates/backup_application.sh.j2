#!/bin/bash

BACKUP_DIR="{{ project_base_dir }}/backups/application"
APP_DIR="{{ project_base_dir }}/application"

mkdir -p $BACKUP_DIR

TIMESTAMP=$(date +"%Y%m%d_%H%M%S")

tar -czf $BACKUP_DIR/application_$TIMESTAMP.tar.gz $APP_DIR

echo "Backup created: $BACKUP_DIR/application_$TIMESTAMP.tar.gz"