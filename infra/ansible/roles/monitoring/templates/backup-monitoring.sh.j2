#!/bin/bash

BACKUP_DIR="/var/backups/monitoring"
DATE=$(date +%Y-%m-%d)

mkdir -p $BACKUP_DIR

tar -czf $BACKUP_DIR/monitoring-$DATE.tar.gz /etc/prometheus /var/lib/grafana

echo "Monitoring backup created: $BACKUP_DIR/monitoring-$DATE.tar.gz"